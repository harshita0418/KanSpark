import { LoginForm } from '@/components/Auth';

export const metadata = {
  title: 'Sign In - Kanspark',
  description: 'Sign in to your Kanspark account',
};

export default function LoginPage() {
  return <LoginForm />;
}
